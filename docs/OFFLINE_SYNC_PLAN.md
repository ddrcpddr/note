# 离线记录与恢复同步方案

更新时间：2026-07-04

当前项目是家庭自用工具，离线能力的第一目标不是复杂协同，而是避免手机临时记录因为 NAS / Docker 服务不可用而丢失。

## 第一版范围

- 支持新建记录在服务不可用时保存到浏览器本地。
- 本地记录显示“待同步到 NAS”。
- 服务恢复后自动把本地队列逐条写入 /api/notes。
- 同步成功后用服务端正式记录替换本地临时记录。
- 富文本 HTML / JSON、纯文本、分类、成员、标签、图片和附件草稿都会进入待同步 payload。

## 暂不做的范围

- 离线编辑已存在的服务端记录。
- 离线删除 / 归档记录。
- 多设备冲突合并。
- 后台 Service Worker 自动同步。
- Android 原生离线数据库。

这些能力等 Web 端新建队列稳定后再做。

## 本地存储策略

- 当前使用 localStorage 保存 home-notes-offline-create-queue-v1。
- 队列项包含 localId、payload、用于页面展示的 note、createdAt。
- payload 与 /api/notes 创建接口保持一致，方便恢复后直接 POST。
- 如果浏览器存储配额不足，页面不崩溃，但该条记录可能无法持久化，需要后续 UI 提示增强。

## 同步规则

1. App 启动时读取本地队列。
2. 如果服务不可用，首页和详情仍显示本地待同步记录。
3. 如果服务恢复为 sqlite 模式，自动执行同步。
4. 同步按队列顺序逐条 POST 到 /api/notes。
5. 每成功一条，就从本地队列移除一条，并替换对应本地临时记录。
6. 如果某条失败，停止后续同步，保留剩余队列，避免丢失。

## 冲突策略

- 第一版只处理“本地新建记录”，没有服务端同一记录的并发修改。
- 本地临时记录未同步前可以继续停留在页面，但暂不保证离线编辑后覆盖原队列项。
- 后续如果做离线编辑，需要给本地记录加版本号和 dirty 字段。

## 人工测试建议

1. 关闭 Docker 或断开服务。
2. 打开前端，新建一条富文本记录，插入图片或附件。
3. 确认详情页显示“待同步到 NAS”。
4. 刷新页面，确认本地记录仍存在。
5. 恢复 Docker 服务。
6. 刷新或等待页面恢复，确认记录自动同步并变成正式记录。

## 后续增强

- 改用 IndexedDB 存储大附件，避免 localStorage 容量限制。
- 增加“手动同步”按钮和失败原因提示。
- 支持离线编辑本地待同步记录。
- 支持服务端记录离线编辑时的冲突检测。

## PWA 前端壳缓存补充

当前已新增 service worker，用于缓存前端壳和静态资源。它不缓存 /api/ 数据。

这意味着：

- 手机或浏览器至少在线打开过一次家事记后，后续 Docker/NAS 暂时不可用时仍有机会打开已缓存的前端壳。
- 离线时新建记录进入本地待同步队列。
- 服务恢复后，前端再把队列同步到 /api/notes。
- 如果从未在线打开过页面，或者浏览器清理了站点缓存，Docker/NAS 不在线时仍无法冷启动页面。


## Docker 验证记录

2026-07-04 已用 Docker 3300 服务验证：

- 容器状态 healthy。
- HTTP smoke 通过，干净测试库为 0 条记录、2 个默认成员、11 个默认分类。
- /sw.js 可访问，并确认只缓存前端壳与静态资源，不缓存 /api/ 数据。


## 离线 app-data 快照

为避免 Docker/NAS 暂时不可用时只能看到空页面，前端现在会在在线成功加载后缓存一个轻量 app-data 快照：

- 最近 100 条非离线临时记录。
- 当前分类列表。
- 默认成员与当前成员。

当 /api/app-data 不可用时，前端会优先读取这个快照并进入 offline-cache 模式。用户仍可以新建记录，新记录进入本机待同步队列。恢复服务后，队列继续自动写入 /api/notes。

限制：该快照使用 localStorage，只适合作为短期兜底；大量图片、附件、长富文本和多设备冲突后续应迁移到 IndexedDB 与更完整的同步策略。

验证补充：离线 app-data 快照代码合入后，Docker 3300 smoke 通过，/sw.js 仍不缓存 /api/ 数据。

## 2026-07-04 本地优先长期离线第一版

离线目标从“短期兜底”升级为“长期离线可使用，恢复联网后同步”。本轮新增 IndexedDB 本地优先数据层，数据库名为 `home-notes-offline-first-v1`。

新增本地 stores：

- `notes`：本机可查看和编辑的记录副本，包含纯文本、富文本 HTML/JSON、分类、成员、标签、附件元数据和同步状态。
- `attachments`：预留给图片/附件 Blob 和元数据，后续用于大附件长期离线。
- `categories`、`members`、`tags`：本地基础数据快照。
- `syncQueue`：待同步的新建/编辑操作。
- `meta`：当前成员、快照时间等轻量状态。

当前第一版行为：

1. 在线成功读取 `/api/app-data` 后，会把服务端记录和基础数据写入 IndexedDB。
2. NAS/Docker/API 不可用时，优先从 IndexedDB 读取本地记录快照，页面进入 `offline-first` 模式。
3. 新建记录先写入 IndexedDB，并进入 `syncQueue`，状态为 `local-only`。
4. 编辑记录先写入 IndexedDB，并进入 `syncQueue`，状态为 `dirty`。
5. 服务恢复为 `sqlite` 模式后，前端会读取 `syncQueue`，逐条 POST/PATCH 到服务端。
6. 同步成功后，本地记录标记为 `synced`，本地临时 ID 会被服务端正式 ID 替换。

当前仍保留旧的 `localStorage` 队列作为迁移兜底，但长期离线主路径已经改为 IndexedDB。

当前限制：

- 大图片和大附件 Blob 的完整 IndexedDB 持久化仍需下一轮增强。
- 多设备同时编辑同一条记录时，第一版不自动合并冲突。
- Service Worker 仍不缓存 `/api/` 响应，避免旧服务端数据污染。
- Android 壳首次冷启动仍需要至少加载过一次 Web 前端壳；完全内置前端资源可以后续再做。
