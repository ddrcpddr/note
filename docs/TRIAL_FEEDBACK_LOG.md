# Trial Feedback Log

本文件记录真实手机 / Docker / NAS 试运行反馈和修复状态。它只记录脱敏现象、复现步骤和 commit，不记录真实 NAS 地址、账号、密码、token、完整笔记正文、真实 `.nsx` 文件内容、数据库、备份、导出或附件。

## 当前试运行状态

- 当前阶段：真实手机 + Docker 试运行稳定化。
- 当前入口：Docker 服务 `http://127.0.0.1:3300/`，手机使用同局域网地址访问；真实 IP 不写入仓库。
- 当前规则：只修真实反馈的小 bug，不新增大功能，不重做 UI，不创建 Android 工程。
- 反馈模板：见 `docs/TRIAL_FEEDBACK_TEMPLATE.md`。

## 反馈总览

| ID | 日期 | 页面 | 严重程度 | 问题摘要 | 状态 | 修复 commit |
| --- | --- | --- | --- | --- | --- | --- |
| TF-20260630-001 | 2026-06-30 | 详情 / 编辑 / 导入 | P1 | 编辑取消标签后仍出现 `待办`；`+ 添加标签` 无反应；导入页“导出文件”文案误导 | 已修复 | `0a52001` |
| TF-20260630-002 | 2026-06-30 | 首页 | P1 | 首页“今天要记 / 快速记录”卡片点击无反应 | 已修复 | `98ad2c9` |

## 已修复反馈

### TF-20260630-001 - 标签编辑与导入页文案

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-06-30 |
| 测试人 | 用户本人 |
| 设备型号 | Android 手机，型号未记录 |
| Android 版本 | 未记录 |
| 浏览器 | 手机浏览器 / PWA，待补充 |
| 访问方式 | Docker 局域网试运行 |
| 页面 | 记录详情、编辑记录、导入 Note Station |
| 操作步骤 | 1. 打开记录详情进入编辑；2. 取消标签后保存；3. 返回详情；4. 点击 `+ 添加标签`；5. 查看导入 Note Station 页初始文案 |
| 预期结果 | 清空标签后不再显示标签；添加标签入口可用；导入页文案不混淆“导入”和“导出” |
| 实际结果 | 标签被强行补回 `待办`；添加标签无反应；导入页出现“选择导出文件”等误导文案 |
| 截图路径 | 用户聊天截图，未提交 Git |
| 是否影响数据 | 是，标签保存结果不符合用户操作 |
| 严重程度 | P1 |
| 是否已复现 | 是，通过代码路径和回归测试复现 |
| 是否已修复 | 是，commit `0a52001` |
| 对应 commit | `0a52001 Fix: respect empty tags and clarify import copy` |

修复摘要：

- 保存记录时允许 `tags: []`，不再兜底为 `待办`。
- `+ 添加标签` 改为轻量输入。
- 导入页文案统一为 `Note Station .nsx 文件`、`选择 .nsx 文件`、`预览导入记录`。
- 验证：`build/check/test`、Docker 重建和 HTTP smoke 通过。

### TF-20260630-002 - 首页今天要记入口无响应

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-06-30 |
| 测试人 | 用户本人 |
| 设备型号 | Android 手机，型号未记录 |
| Android 版本 | 未记录 |
| 浏览器 | 手机浏览器 / PWA，待补充 |
| 访问方式 | Docker 局域网试运行 |
| 页面 | 首页 |
| 操作步骤 | 1. 打开首页；2. 点击“今天要记 / 快速记录”卡片 |
| 预期结果 | 进入“新记录”页，可以快速记一条 |
| 实际结果 | 点击后没有任何反应 |
| 截图路径 | 用户聊天截图，未提交 Git |
| 是否影响数据 | 否，但影响核心新建入口体验 |
| 严重程度 | P1 |
| 是否已复现 | 是，`TodayCard` 原本是静态 `<section>`，无点击 handler |
| 是否已修复 | 是，commit `98ad2c9` |
| 对应 commit | `98ad2c9 Fix: wire home quick record action` |

修复摘要：

- App 向 `HomeScreen` 传入 `() => navigate('new')`。
- `HomeScreen` 将 `onCreateNote` 传给 `TodayCard`。
- `TodayCard` 改成保持 V1 样式的可点击按钮。
- 新增 `tests/frontend-ui.test.js` 覆盖首页快捷入口。
- 验证：`build/check/test`、Docker 重建和 HTTP smoke 通过。

## 待复现 / 待修复反馈

当前无未处理反馈。后续新增反馈请按 `docs/TRIAL_FEEDBACK_TEMPLATE.md` 复制条目到本节，再按 P0/P1/P2/P3 排序处理。

## 反馈处理规则

- P0：立即停止试运行中高风险操作，先保护数据，再修复。
- P1：使用 `mvp-bugfix-qa` 流程，一个 bug 一个 `Fix:` commit。
- P2：可排队，小步修复，不重做 UI。
- P3：等一轮试运行结束后统一做微调，不抢占稳定性修复。

## 下一步

继续真实手机试运行。下一批反馈建议优先覆盖：

1. 新建记录保存和刷新不丢失。
2. 搜索新建记录。
3. 查看 Note Station 导入记录详情。
4. 设置页手动备份、JSON 导出、Markdown 导出。
5. 小附件上传。
6. PWA 添加到桌面后的启动体验。